<h2>Did you want to modify <a href="/foods/<%= @food.id %>/edit" ><%= @food.name %></a>?</h2>

	<ul>
		<% @parties.each do |name| %>
		<li>
		<%= name.name %> has ordered this.
		</li>
		<% end %>
	<ul>
<form action="/foods/<%= @food.id %>" method="post"/>
<input type="hidden" name="_method" value="delete" />
<input type="submit" value="Goodbye Food">
</form>